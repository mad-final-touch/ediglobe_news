<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.ediglobe_news">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ediglobe_news">

        <activity
            android:name="com.example.ediglobe_news.NewsDetailActivity"
            android:exported="false"> <!-- Or true, depending on your needs -->
            <!--
                         You might also need an <intent-filter> here if you want
                         other apps to be able to start this activity directly,
                         or if it responds to specific implicit intents.
            -->
        </activity>
        <activity
            android:name="com.example.ediglobe_news.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>